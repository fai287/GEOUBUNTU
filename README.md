-This is a Django fully funtional community web-based application for geospatial devs

--To run this project:
       --set up your virtual environment
      --pip install django
      --installl jupterlab
    --run the application using "python manage.py runserver"
